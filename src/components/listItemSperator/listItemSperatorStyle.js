import {StyleSheet} from 'react-native';
import colors from '../../config/colors'
const ListItemSperatorStyle = StyleSheet.create({
  sperator: {width: '100%', height: 1, backgroundColor: colors.lightGray},
});
export default ListItemSperatorStyle;