import {StyleSheet } from 'react-native'
import colors from '../../config/colors';
const ListItemDeleteStyle = StyleSheet.create({container:{
    width:70,
    justifyContent:'center',
    alignItems:'center',
    backgroundColor:colors.danger,
    color:colors.white,
}})

export default ListItemDeleteStyle;